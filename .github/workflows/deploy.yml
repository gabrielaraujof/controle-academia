name: Continuous Deployment

on:
  push:
    branches: [main]

jobs:
  ci:
    name: CI
    runs-on: ubuntu-20.04
    uses: ./.github/workflows/default.yml

  dev:
    name: Deploy
    runs-on: ubuntu-20.04
    needs: [ci]
    steps:
      - run: echo Works!
